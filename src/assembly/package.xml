<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.0
          http://maven.apache.org/xsd/assembly-2.1.0.xsd">
    <id>package</id>
    <formats>
        <format>dir</format>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <!-- Copy manifest.json to root -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>manifest.json</include>
            </includes>
        </fileSet>

        <!-- Copy documentation (LICENSE, README.md, version.txt) -->
        <fileSet>
            <directory>${project.basedir}/doc</directory>
            <outputDirectory>doc/</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
        </fileSet>

        <!-- Copy config examples to etc/ -->
        <fileSet>
            <directory>${project.basedir}/config</directory>
            <outputDirectory>etc/</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
        </fileSet>

        <!-- Copy assets (logo and images only, no subdirectories) -->
        <fileSet>
            <directory>${project.basedir}/assets</directory>
            <outputDirectory>assets/</outputDirectory>
            <includes>
                <include>*</include>
            </includes>
        </fileSet>
    </fileSets>

    <!-- Copy all runtime dependencies to lib/ (excluding Kafka Connect framework) -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>lib/</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <scope>runtime</scope>
            <excludes>
                <exclude>org.apache.kafka:*</exclude>
                <exclude>org.slf4j:*</exclude>
            </excludes>
        </dependencySet>
    </dependencySets>
</assembly>
